"""Legacy pre-v0 Schema tests retired.

The current Closure DNA surface is Database/Table/SQL, not the removed
Schema wrapper that these tests originally targeted.
"""

from __future__ import annotations

import pytest

pytest.skip("legacy Schema-based tests retired for the current Closure DNA surface", allow_module_level=True)
